#include <bits/stdc++.h>
using namespace std;

int randomNum(int a, int b) {
    srand(time(NULL));
    int res = 0, range = b - a, bits = ceil(log(range));
    for (int i = 1; i <= bits; i++) {
    }
}
